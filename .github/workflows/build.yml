name: Android Play Store Build

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install System Dependencies (Fix Aidl)
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential git python3-dev aidl libffi-dev libssl-dev

      - name: Build with Buildozer
        # Großschreibung bei ArtemSerebrennikov ist korrigiert
        uses: ArtemSerebrennikov/buildozer-action@v1
        with:
          command: buildozer android release
          buildozer_version: master

      - name: Upload AAB Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Vortex-Lotto-Bundle
          path: bin/*.aab
